// This route group is currently unused. Public pages use the PublicShell component directly.
// Kept for potential future use with route-group-based layouts.
export default function PublicGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
